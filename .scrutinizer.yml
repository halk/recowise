build:
    tests:
        override:
            -
                command: nosetests
                coverage:
                    file: .coverage
                    config: .coveragerc
                    format: py-cc
